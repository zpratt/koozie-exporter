import React from 'react';
import Head from 'next/head';

export const Home = () => {
    return (
        <div className='container'>
            <Head>
                <title>Topokube</title>
            </Head>
        </div>
    );
};

export default Home;
